listen_ip = "127.0.0.1"
listen_port = 8787
tmdb_api_key = "legacy-tmdb-test-key"
mteam_api_key = "legacy-mteam-test-key"
douban_cookie = "dbcl2=legacy-test-only; ck=test"

[[qb_servers]]
name = "legacy-nas"
base_url = "http://127.0.0.1:8080"
username = "admin"
password = "legacy-test-password"
insecure_tls = false

[subscription_watcher]
poll_interval_secs = 3600
library_limit = 200
max_retries = 3
bootstrap_existing_as_skipped = true
